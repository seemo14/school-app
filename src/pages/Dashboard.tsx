export default function Dashboard() {
  return (
    <div className="p-6">
      <h2 className="text-xl font-semibold">Dashboard OK ✅</h2>
      <p className="mt-2">If you can see this, the render loop is not from routing.</p>
    </div>
  );
}
